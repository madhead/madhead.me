name: Dead links

on:
  schedule:
    - cron: '0 4 * * *'
  workflow_dispatch:

jobs:
  dead-links:
    name: Dead links
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        site:
          - https://madhead.me
          - https://ru.madhead.me
    steps:
      - name: ${{ matrix.site }}
        uses: ruzickap/action-my-broken-link-checker@v2.6.0
        with:
          url: ${{ matrix.site }}
          cmd_params: >-
            --rate-limit=10
            --max-connections-per-host=5
            --buffer-size=8192
            --ignore-fragments
            --exclude="localhost"
            --exclude="linkedin"
            --exclude=".*madhead\.me\/cdn-cgi\/l\/email\-protection.*"
